// interface class is used to store data in varibale and then binding that variable into HTML.
// OR we can directly create an Observable(sync Pipe)

export interface todo{
    id:number;
    value: string;
    isDone: boolean;
}